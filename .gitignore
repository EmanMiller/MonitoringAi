# Environment and secrets (Dan loads .env locally only)
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Dan coordination flags (local only, not committed)
.dan-hold
.dan-approved

# Dan watcher runs from repo root; keep existing ignores in subprojects
# (DashboardFrontend and DashboardApi have their own .gitignore)

# QA, task breakdowns, and review docs (local/coordination only)
QA_REPORT.md
QA_SIGN_OFF_REPORT.md
RACHEL_REVIEW.md
task-cards/
docs/BECCA_CARD*_E2E_CHECKLIST.md
